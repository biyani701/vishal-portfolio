# Local defaults for `pnpm dev` only (public, non-secret). Override in .env.development.local.
# apps/auth-server's dev script uses port 4000; apps/api's port is settled when it is scaffolded (P10.1).
VITE_API_BASE_URL=http://localhost:3001
VITE_AUTH_SERVER_URL=http://localhost:4000
