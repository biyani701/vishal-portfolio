// Runtime overrides for the build-time VITE_* configuration (keys: src/config/schema.ts).
// Loaded synchronously before the app, so a deploy can repoint the site without rebuilding by replacing
// this file, e.g. window.runtimeConfig = { API_BASE_URL: "https://api.vishal.biyani.xyz" }.
// Leave it empty to use the build values.
window.runtimeConfig = {}
