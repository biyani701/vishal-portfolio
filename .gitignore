node_modules
.pnpm-store
.turbo

# portfolio (CRA)
apps/portfolio/build

# auth-server (Next.js)
apps/auth-server/.next
apps/auth-server/out

# env
.env
.env.local
.env.*.local
!.env.production

# misc
.DS_Store
*.log

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
*.gate.lock*

# apps/web + apps/api build and test output (rebuild-portfolio-app)
apps/*/dist
.vercel
playwright-report/
test-results/
blob-report/
